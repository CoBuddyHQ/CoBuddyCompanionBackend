# =============================================================
# CoBuddy Companion Backend — Docker Compose (Development)
# Usage:
#   docker compose -f docker-compose.dev.yml up -d --build
# =============================================================

services:
  postgres:
    image: postgres:16-alpine
    container_name: cobuddy_companion_postgres_dev
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-cobuddy_companion_db}
      POSTGRES_USER: ${POSTGRES_USER:-cobuddy}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-development-secret-password-2026}
    ports:
      - "5432:5432"
    volumes:
      - cobuddy_companion_postgres_data_dev:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-cobuddy} -d ${POSTGRES_DB:-cobuddy_companion_db}"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    networks:
      - cobuddy_companion_network_dev

  redis:
    image: redis:7-alpine
    container_name: cobuddy_companion_redis_dev
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - cobuddy_companion_redis_data_dev:/data
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 5s
    networks:
      - cobuddy_companion_network_dev

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cobuddy_companion_backend_dev
    restart: unless-stopped
    environment:
      NODE_ENV: development
      PORT: 4001
      DATABASE_URL: postgresql://${POSTGRES_USER:-cobuddy}:${POSTGRES_PASSWORD:-development-secret-password-2026}@postgres:5432/${POSTGRES_DB:-cobuddy_companion_db}?schema=public
      REDIS_URL: redis://redis:6379
      JWT_SECRET: ${JWT_SECRET:-cobuddy-companion-super-secret-jwt-key-2026}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:-cobuddy-companion-super-secret-refresh-key-2026}
      RAZORPAY_KEY_ID: ${RAZORPAY_KEY_ID:-rzp_test_TLCpKIamWxQBYy}
      RAZORPAY_KEY_SECRET: ${RAZORPAY_KEY_SECRET:-w2l3tVjYk6aH4zM9xQ8pL7nR}
      OTP_DEV_BYPASS: ${OTP_DEV_BYPASS:-123456}
      CORS_ORIGIN: "*"
    ports:
      - "4001:4001"
    volumes:
      - ./src:/app/src
      - ./prisma:/app/prisma
      - ./uploads:/app/uploads
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - cobuddy_companion_network_dev

volumes:
  cobuddy_companion_postgres_data_dev:
    driver: local
  cobuddy_companion_redis_data_dev:
    driver: local

networks:
  cobuddy_companion_network_dev:
    driver: bridge
    name: cobuddy_companion_network_dev
